def swap(x, y):
    if not (isinstance(x, (int, float)) and isinstance(y, (int, float))):
        return -1

    # Swap using arithmetic operations (no extra variables)
    x = x + y
    y = x - y
    x = x - y

    return x, y

print(swap("Apple", 10))
print(swap(9, 17))